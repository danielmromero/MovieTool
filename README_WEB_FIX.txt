Deploy notes:
- Put these files at the root of the GitHub repo, not inside another folder.
- On Render, Root Directory should be blank or set to .
- Start Command: python server.py
- Health Check Path: /health


Auto-refresh: the catalog refreshes itself every 2 days when the service is running.
Manual refresh: use the in-app refresh button and enter the password GetTheNewStuff.
